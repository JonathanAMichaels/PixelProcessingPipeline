---
# Configuration file
# Auto-generated parameters
# path of working directory (must be provided in command line)
folder:
# path to neuropixel directory
neuropixel:
# number of neuropixels recorded
num_neuropixels:
# path to neuropixels channel map (should be a .mat file, see 'geometries' folder)
neuropix_chan_map_file: neuropixPhase3B1_kilosortChanMap.mat
# path to myomatrix directory (should end in _myo)
myomatrix:
# path to myomatrix channel map (should be a .mat file, see 'geometries' folder)
myo_chan_map_file: bipolar_test_kilosortChanMap.mat
# path to .kinarm files
kinarm:
# path to pipeline.py script (always overwritten by program)
script_dir:

# Registration parameters
Registration:

# Sorting parameters
Sorting:

# Session-specific parameters
Session:
    trange:
        - 0
        - 0
    myo_chan_list:
        - [1, 16]
        - [17, 32]
    myo_analog_chan: 33
    myo_muscle_list:
        - Bicep
        - Tricep
    brain_coord_list:
        - [1, 1]
        - [2, 2]
    brain_area_list:
        - M1
        - PMd

